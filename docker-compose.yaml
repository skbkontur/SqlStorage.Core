version: "3.3"
services:
    postgres:
        container_name: postgres
        image: postgres:11.4
        restart: always
        environment:
            POSTGRES_USER: postgres
            POSTGRES_PASSWORD: postgres
        ports:
            - 5432:5432